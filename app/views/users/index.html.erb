<h1>
	<%=pluralize(@users.size, "User") %>
</h1>
<ul class="users">
	<% @users.each do |user| %>
		<li>
			<%= link_to user.name, user %>
			Member since 
			<%= user.created_at.strftime("%B %Y") %>
		</li>
	<% end %>
</ul>	